# Escreva um programa que leia 10 números inteiros e os armazene em uma lista.
# Imprima a lista, o maior elemento e a posição que ele se encontra.

lista = []
for i in range(10):
    x = int(input())
    lista.append(x)

print(lista)
print(max(lista))
posicao = lista.index(max(lista))
print(posicao)
