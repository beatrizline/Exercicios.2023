# Escreva um programa que leia um preço e um valor percentual. 
# Informe o preço com o aumento percentual e o preço com o desconto percentual. 
# Por exemplo, se for lido um preço de 100.00 e o valor percentual de 5 o programa deve mostrar que o preço com aumento é 105.00 e o preço com desconto é 95.00.

def preco (p,p2):
    return p + (p*(p2/100))

def desconto (p,p2):
    return p - (p*(p2/100))

p = float(input())
p2 = float(input())
preco_aumento = preco(p, p2)
preco_desconto = desconto(p, p2)

print(f'{preco_aumento:.2f}')
print(f'{preco_desconto:.2f}')
